apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${APP_PREFIX}-env-runner-k8s
  namespace: ${NAMESPACE}
---
# Subset of cp's RBAC that the KubernetesProvider actually uses:
# pods/services/events/pvcs + deployments. (No secrets/configmaps — the afs
# ConfigMap is referenced by name in the pod spec, never read via the API.)
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ${APP_PREFIX}-env-runner-k8s
  namespace: ${NAMESPACE}
rules:
  # secrets: the runner writes each workspace's own cp token into a per-workspace
  # Secret before bringing the pod up, and removes it on destroy.
  - apiGroups: [""]
    resources: ["pods", "services", "events", "persistentvolumeclaims", "secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  # statefulsets: the auto-scaling workload shape. Only used where the storage
  # class supports ReadWriteMany, but granted unconditionally so enabling that
  # does not also require a permissions change.
  - apiGroups: ["apps"]
    resources: ["deployments", "deployments/scale", "statefulsets", "statefulsets/scale"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ${APP_PREFIX}-env-runner-k8s
  namespace: ${NAMESPACE}
subjects:
  - kind: ServiceAccount
    name: ${APP_PREFIX}-env-runner-k8s
    namespace: ${NAMESPACE}
roleRef:
  kind: Role
  name: ${APP_PREFIX}-env-runner-k8s
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${APP_PREFIX}-env-runner-k8s
  namespace: ${NAMESPACE}
  labels:
    app: ${APP_PREFIX}-env-runner-k8s
spec:
  replicas: 1
  # Recreate (not RollingUpdate): the runner mutates cluster state, so two
  # overlapping pods during a deploy could double-apply. Kill the old one first.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ${APP_PREFIX}-env-runner-k8s
  template:
    metadata:
      labels:
        app: ${APP_PREFIX}-env-runner-k8s
    spec:
      serviceAccountName: ${APP_PREFIX}-env-runner-k8s
      containers:
        - name: env-runner-k8s
          image: ${PLATFORM_IMAGE_PREFIX}-env-runner-k8s:${IMAGE_TAG}
          imagePullPolicy: Always
          # Provider config MUST match control-plane.yaml exactly, so both build
          # byte-identical workspace pods. Keep this block in sync.
          env:
            - name: K8S_NAMESPACE
              value: "${NAMESPACE}"
            - name: AGENT_IMAGE_PREFIX
              value: "${AGENT_IMAGE_PREFIX}"
            - name: AGENT_IMAGE_TAG
              value: "${AGENT_IMAGE_TAG}"
            - name: AGENT_STORAGE_CLASS
              value: "${AGENT_STORAGE_CLASS}"
            - name: IMAGE_PULL_SECRET
              value: "${IMAGE_PULL_SECRET}"
            - name: AGENT_NODE_SELECTOR
              value: "${AGENT_NODE_SELECTOR}"
            # Injected into workspace pods as CP_URL (k8s-provider). Without it
            # the code falls back to http://nap-cp:3000, which only resolves
            # when APP_PREFIX is "nap". Keep in sync with control-plane.yaml.
            - name: CP_SERVICE_URL
              value: "http://${APP_PREFIX}-cp:3000"
            - name: AFS_ENABLED
              value: "true"
            - name: AFS_IMAGE
              value: "${AFS_IMAGE}"
            - name: AFS_CONTROLLER_ADDR
              value: "afs-controller.${NAMESPACE}.svc:9100"
            - name: MEMORY_FUSE_IMAGE
              value: "${PLATFORM_IMAGE_PREFIX}-memory-fuse:${IMAGE_TAG}"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ${APP_PREFIX}-pg-user
                  key: password
            - name: DATABASE_URL
              value: "postgresql://${PG_USERNAME}:$(POSTGRES_PASSWORD)@${APP_PREFIX}-pg-rw:5432/${DB_NAME}"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
