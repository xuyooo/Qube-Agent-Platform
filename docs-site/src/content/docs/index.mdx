---
title: Start here
description: Install Qube Agent Platform on one machine, then find your way around the docs.
tableOfContents: false
---

One Linux machine, one command:

```bash
curl -sfL https://xuyooo.github.io/Qube-Agent-Platform/get.sh | sudo sh -
```

It installs a single-node k3s cluster and the whole platform on it, then prints the URL and the admin credentials. Around five minutes. Overrides and the existing-cluster install are in the [single-node quickstart](/self-host/single-node/).

<div class="nap-paths">
  <a class="nap-next" href="/guides/1-setup/">
    <em>Next step</em>
    <strong>Run your first agent</strong>
    <span>Set up a provider, finish your first conversation — about 10 minutes once the platform is up</span>
  </a>
  <a class="nap-ref" href="/concepts/overview/">
    <em>Reference</em>
    <strong>Concepts</strong>
    <span>The mental model: agents, workspaces, providers, routes</span>
  </a>
</div>
